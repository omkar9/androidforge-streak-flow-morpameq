<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- AdMob requires ACCESS_NETWORK_STATE and INTERNET, which are already listed. -->

    <application
        android:name=".StreakFlowApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreakFlow" <!-- Default theme, will be overridden by splash theme -->
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting"> <!-- Use the splash screen theme for Android 12+ -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdMob Configuration -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/> <!-- AdMob Test App ID -->

        <!-- Notification Broadcast Receiver -->
        <receiver
            android:name=".data.notifications.HabitReminderBroadcastReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Boot Receiver for re-scheduling notifications after device reboot -->
        <receiver
            android:name=".data.notifications.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <!-- For API 26+ devices, implicit broadcasts for BOOT_COMPLETED are restricted.
                     Consider using WorkManager for more robust scheduling if needed,
                     but for basic alarms, this is still common. -->
            </intent-filter>
        </receiver>

    </application>
</manifest>